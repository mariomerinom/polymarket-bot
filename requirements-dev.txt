# Local-only dev dependencies. NOT installed on the VPS engine.
# Used by tools/diag.py (Streamlit diagnostic dashboard).
#
# Install: source venv/bin/activate && pip install -r requirements-dev.txt

streamlit>=1.28
plotly>=5.18
pandas>=2.0
