# Session Log — 2026-04-15

## Summary

Graduate VWAP mean-reversion to perp pipelines + expand ETH HIGH_VOL gate. Two data-driven changes from the 23K-prediction lab report.

## Changes Shipped

### 1. VWAP Mean-Reversion Graduation (ci_run_perp.py)

**What:** In mean-reverting regimes where momentum skips, VWAP now fires as a secondary signal. Conviction >= 3 (z-score >= 2.0) stores a real prediction; lower conviction still skips.

**Why:** Lab showed strong edge on SOL (55.2% WR, 531 bets, +$1,375) and DOGE (53.4%, 532 bets, +$900). Both well past 200-bet gate. Momentum and VWAP are complementary — zero overlap.

**Scope:** All perp pipelines (SOL, DOGE, ETH, BTC on Bybit/HL). Regime determines when it fires, not per-asset config.

**Revert:** WR < 52% on 50+ bets for any asset.

### 2. ETH HIGH_VOL Gate Expansion (predict_eth.py)

**What:** Changed `"HIGH_VOL" in regime_label and "TRENDING" not in regime_label` to `"HIGH_VOL" in regime_label`. Now blocks ALL HIGH_VOL regimes for ETH.

**Why:** HIGH_VOL/TRENDING leaked through at 37.5% WR (40 bets), HIGH_VOL/NEUTRAL at 39.3% (28 bets). BTC handles HV fine (54-63%) — ETH-specific problem.

**Revert:** Counterfactual HV/TRENDING WR > 55% on 50+ shadow bets.

### 3. Optimization Registration

Both experiments registered in `docs/optimizations.json`:
- `vwap_graduation_perps` — active, min 50 bets
- `eth_highvol_full_gate` — active, min 50 bets

### 4. Test Update

Updated `test_highvol_non_trending_gate_eth` to expect conv=2 for HV/TRENDING (was conv=3).

## Issues Filed

- #78 — Decision: Graduate VWAP mean-reversion to perps (SOL/DOGE)
- #79 — Decision: Monitor VWAP on ETH/BTC perps (untargeted)
- #80 — Decision: Expand ETH HIGH_VOL gate to all HIGH_VOL

## Deployment

- Pulled on VPS, restarted botsy.service — active
- 701 tests passed, 0 failed

## What to Watch

1. **VWAP perps** — first VWAP signals should appear in next MR regime. Check `Signal: VWAP DOWN (z=+X.X, conv=3)` in logs.
2. **ETH HV gate** — ETH predictions in HIGH_VOL/TRENDING should now show conv=2 instead of conv=3.
3. **At 50 bets:** Run `mcp__botsy__win_rate` for VWAP predictions and ETH HV regime.
